node {
   echo 'start build'
   stage('Get GitHub'){
      git 'https://github.com/ydcqy/cloud-test.git'
   }
   mvnHome = tool 'M3'
   stage('Maven Build'){
      sh "${mvnHome}/bin/mvn} clean package"
   }
   stage('Finished')
      echo 'success!'
}